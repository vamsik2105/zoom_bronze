name: 'Zoom_Customer_Analytics'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target"]

models:
  Zoom_Customer_Analytics:
    bronze:
      +materialized: table
