version: 2

sources:
  - name: raw
    database: RAW
    schema: RAW
    tables:
      - name: users

models:
  - name: bz_simple_test
    description: "Simple test model"
    columns:
      - name: test_id
        description: "Test ID"
      - name: test_value
        description: "Test value"
