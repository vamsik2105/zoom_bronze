version: 2

sources:
  - name: raw
    schema: RAW
    tables:
      - name: region
      - name: customer
      - name: orders

models:
  - name: audit_log_bz
    description: "Audit log table for bronze layer"
  - name: region_bz
    description: "Bronze layer region data"
  - name: customer_bz
    description: "Bronze layer customer data"
  - name: orders_bz
    description: "Bronze layer orders data"
